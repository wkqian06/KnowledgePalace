# Gaps INDEX

<!-- One row per gap card. Used for prefilter (grep concepts) and status overview.
     Relation counts are never stored — compute live: grep "gap-<slug>" papers/*.md -->

| Slug | Type | Status | Source | Concepts | Updated |
|---|---|---|---|---|---|
